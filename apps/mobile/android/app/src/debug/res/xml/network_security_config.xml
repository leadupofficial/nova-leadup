<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only override. Debug builds target the local dev API over plain HTTP
  (ApiConfig._defaultDevUrl is http://10.0.2.2:3001), which the release policy in
  src/main/res/xml/network_security_config.xml forbids.

  Cleartext stays disabled by default here too; it is re-enabled ONLY for the
  emulator/loopback hosts used in development. Release builds never see this file.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator alias for the host machine's loopback. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- Genymotion's equivalent. -->
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
