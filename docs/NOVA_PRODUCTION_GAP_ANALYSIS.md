# NOVA — Production Gap Analysis

Every row is a gap between the intended product and observed behaviour, with severity,
root cause, and what remains. Status legend: **FIXED** (code changed + re-verified),
**OPEN**, **BLOCKED** (needs an asset or decision I cannot supply).

| # | Feature | Expected | Current (observed) | Root cause | Sev | Fix | Remaining work | Production impact |
|---|---|---|---|---|---|---|---|---|
| 1 | Reminder creation in Indian languages | Any supported language creates the reminder | Hindi/Telugu/Kannada/Bengali turns failed the tool 2–3× and hit the iteration cap; Telugu answered in English with "That didn't work" | `stated-time.ts` clock-time guard knew only English + Tamil wording, so a stated hour read as *invented* | **P0** | **FIXED** — added o'clock markers + parts of day for 11 Indian languages, Latin transliterations, Tamil phonetic `ஓ கிளாக்`; guard still refuses time-less turns | Widen to remaining catalogue languages if their STT is enabled | The product's core action was unusable in 4 of 5 tested languages |
| 2 | Hinglish/Benglish/Gujlish speech recognition | Mixed speech understood | "Kal subah aath baje ka reminder laga do yaar" → **"Reminder lag"** | `getSttProviderForLanguage` special-cased only `tanglish`; other mixed codes fell to Deepgram (English) | **P0** | **FIXED** — all mixed codes route to Sarvam; same for voice provider | — | Mixed-language users lost most of every sentence |
| 3 | Honesty about completed actions | Never claim an action that did not happen | "ரிமைண்டர் வெச்சுடுச்சு" ("the reminder is set") with `tools: []` and no row | `claimsStateChange` missed the Tamil completive `-ச்சுடுச்சு / -ட்டு / -ஞ்சு` stems | **P1** | **FIXED** — stems added in Tamil + Latin; present-tense descriptions still ignored | Audit other reply shapes as new phrasings appear | User believes a reminder exists and misses the commitment |
| 4 | Push transport for proactive nudges | A nudge reaches the phone | **FIXED & VERIFIED ON DEVICE 2026-09-23.** The phone registered a real FCM token (`devices.push_token`, 142 chars); `notificationService.create` logged `devices: 1, sent: 1, failed: 0, configured: true`; with the app backgrounded the shade showed *"NOVA — Call Arun — You have a reminder to call Arun…"*. | `devices.push_token` existed and the console reported `hasPushToken`, but no route accepted a token and no code sent a message; the app never called `/notifications` | **P0** | **FIXED** — `/device/register` stores the token, `services/fcm.ts` sends over FCM HTTP v1, `NotificationService.create` pushes after writing the row | See 4a–4c | Every nudge stayed a row in a table |
| 4a | Foreground push | A nudge is visible with the app open | **FIXED & VERIFIED.** FCM accepted the send and the shade stayed empty; the identical message appeared the instant the app was backgrounded. After the fix it posts with `id=900001`, `channel=nova_reminders`, `importance=4`. | Android posts a system notification only when the app is backgrounded; with NOVA open the message went to `onMessage` and nobody drew it | **P1** | **FIXED** — `PushForegroundHandler` draws it through the existing local-notification channel | — | The nudge was delivered and dropped |
| 4b | In-app notification surface | The user can read and clear nudges | **FIXED & VERIFIED ON DEVICE 2026-09-23.** The bell shows the real unread count (**4**), tapping it opens an inbox listing every nudge with title, body and relative time; tapping one wrote the read flag through (database then held 1 read / 3 unread, the row rendered as read, the badge fell to **3**); dismissing one removed the row (4 → 3). | `/notifications` existed with the app calling none of it, and the bell was hardcoded to `0` | **P1** | **FIXED** — model + four client methods, two providers, an inbox sheet, and the bell rewired | — | A dismissed shade meant the nudge was unrecoverable inside NOVA |
| 4e | Unread count parse | The badge reflects the server | **FIXED.** The count read `data['data']` while the client's `_get` already unwraps the envelope, so every call answered 0 and the badge was empty despite a real count of 4. | Envelope unwrapped twice | **P2** | **FIXED** — `parseUnreadCount` handles the unwrapped payload and a raw envelope, and is total (a non-numeric value leaves the badge empty instead of throwing in `build`) | — | The badge could never tell the user anything |
| 4c | Reminder notifications are silent | A reminder announces itself | **FIXED & VERIFIED ON DEVICE 2026-09-23.** The channel is created with `mSound=content://settings/system/notification_sound`, `mVibrationEnabled=true`, `mShowBanner=true`; a reminder fired into the *alerting* section of the shade ("NOVA reminder — Check the oven") rather than under "Silent". | The channel was created with `Importance.high` but no sound. Setting `playSound` on the installed id would never have taken effect — **Android never updates an existing channel** — so the id moved to `nova_reminders_v2` in the app *and* in the FCM payload (Android drops a notification whose channel does not exist). | **P2** | **FIXED** | — | Every reminder on every install arrived silent |
| 4f | Reminder punctuality | A reminder fires at the time asked for | **FIXED & VERIFIED 2026-09-23.** Reminders were armed with inexact windows (`windowLength 83734` ≈ 84 s, and 87–294 s late after a reboot). The cause was the scheduling mode: the permission probe reported `exact=true`, the app requested `exactAllowWhileIdle`, the plugin returned without throwing, and the OEM batched it anyway. `AndroidScheduleMode.alarmClock` is honoured — `windowLength 0` — and a reminder due at **04:30:26 fired at 04:30:28**. The grant is still required; with it off the alarm returns to a windowed one. | This OEM ignores the `exact*` modes the plugin asks for | **P2** | **FIXED** — rows 36 and 46 carry the measurements | — | "Remind me at 6 PM" now arrives at 6 PM |
| 4g | Reminder response loop | The user can answer the nudge | **WORKS — VERIFIED ON DEVICE 2026-09-23.** A fired reminder → *Tap to talk* → the MacBook said **"Done. I already called the plumber."** → transcribed correctly → NOVA proposed `cancel_reminder` on the exact reminder id behind the approval gate → approved → `reminders.dismissed = true` in the database. | — | **P1 → CLOSED** | — | — | The mandate's central reminder requirement is now demonstrated |
| 4h | Home's "Tap to talk" | Starts a voice session | **FIXED & VERIFIED.** It only navigated: tapping it landed on Converse in the READY state with nothing listening, and the spoken reply produced no turn. | `onPressed: () => context.go('/converse')` — the label promised talking and the action was navigation. Only the wake word path called `startTurn`. | **P1** | **FIXED** — the CTA starts a real turn in the pinned language | — | The primary affordance on Home did not do what it said |
| 4i | Floating orb summon | Starts a voice session | **FIXED.** `_summon` set the avatar to "listening" and navigated, so a session *looked* started while nothing listened. | Same missing `startTurn` call | **P1** | **FIXED** — the orb opens the session too, skipping the call outside a router so a widget test cannot open a socket | Re-verify on a screen other than Home | A cosmetic state change passed for a session |
| 4j | Conversational actions in history | The interaction is recorded | **FIXED & VERIFIED.** Cancelling a reminder by voice set `dismissed = true` and wrote **no** history row; `/activity` reads `audit_logs` and the tool executor wrote zero rows. After the fix, `/activity` returned `reminder.create \| reminder \| cde4950e-… \| success` for a spoken command. | Recording was hooked on `executeToolUses`, which the realtime loop never calls — it calls `executeAssistantTool` directly | **P1** | **FIXED** — hooked at the single funnel both paths share, best-effort so a logging failure cannot report a completed action as failed | — | Every voice-driven action was invisible in history |
| 4d | Delivery receipt | The operator can tell a nudge was delivered | No `delivered_at` on `notifications`; nothing records whether FCM accepted a message | Column never added | **P2** | OPEN | Record the FCM result per notification | 'Sent' cannot be distinguished from 'received' |
| 5 | Wake word "Hey Nova" | Responds to the phrase, in and out of app | **WORKS — VERIFIED ON DEVICE 2026-09-23.** `SherpaWakeWord: DETECTION! hey_nova (keyword=HEY NOVA)` then `WakeWordService: Wake word detected: hey_nova (score=1.0)` — in the foreground *and* backgrounded on the launcher, from the MacBook's speakers through the phone's microphone. Foreground detection opened a live conversation that transcribed and answered in Telugu; a background detection posted *"NOVA — Heard 'Hey Nova' — tap to talk"*. | **The earlier reading was wrong**: the service is not broken, it is **off until the user opts in** (`arm()` returns early when `state.enabled` is false), and the previous check ran without opting in. Once enabled the FGS starts with `foregroundServiceType=microphone` and the real sherpa-onnx KWS model. | **P0 → CORRECTED** | **No code fix needed for detection.** The one real defect found was the notification's broken promise — see 5a | 5a, 5b | — |
| 5a | "Tap to talk" notification | Tapping starts the conversation | **FIXED & RE-VERIFIED.** Tapping it opened NOVA on **Home** with no session, so the wording was untrue. Now it lands on **Converse already LISTENING** with the mic live. | The notification's intent is `getLaunchIntentForPackage` (it only launches the app) and nothing acted on the detection when the app came forward | **P1** | **FIXED** — `_onResume` honours a fresh background detection via `shouldOpenSessionFromWakeWord`, bounded by `wakeWordSessionFreshness` (90s) so a stale detection cannot ambush a later visit | — | A notification that promised an action and did nothing |
| 5b | Wake word is offered during onboarding | A new user is asked | **FIXED & VERIFIED ON DEVICE 2026-09-23.** Onboarding never asked, so every new user finished setup with the wake word off; the only route to it was Profile → Wake word listening. Home compounded it by advertising `or say "Hey Nova"` while it was off (fixed in round 17). A **wakeWord** step now sits after *companion* and before *health*, so the finish line does not move: `welcome → permissions → profile → companion → wakeWord → health → complete`. | Onboarding had no wake word step | **P1** | **FIXED** — verified on the handset: the step renders at `/onboarding/wakeword`, and advancing from companion reaches it (`PUSH onboarding-wakeword prev=onboarding-companion`). Skipping is first-class: the CTA reads "Not now" while off and "Continue" once on. Two tests pin the index order and contiguity, which is what an insert would silently break. | — | The flagship affordance was never offered |
| 6 | Background listening | Continue while backgrounded | `appops RECORD_AUDIO: foreground`; no battery-optimisation exemption; not on Doze whitelist; `BootReceiver` returns early on API ≥ 34 | Design + platform restrictions not handled | **P0** | OPEN (partly by design) | Foreground service with `microphone` type + `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` prompt + boot restart; document what is genuinely impossible on OxygenOS | No background assistant turns |
| 7 | Notification permission in onboarding | Button raises the OS dialog | Three taps produced no dialog; `POST_NOTIFICATION: ignore`; Android recorded **no user decision** | Unconfirmed — `permission_handler` request path or a silently-denied prior attempt | **P1** | OPEN | Instrument `requestNotification()` result; if it returns `denied` permanently, route the user to app settings with an explanation | Reminders fire but are never shown |
| 8 | Permission state freshness | Reflect OS state on return | OS `granted=true` while the app showed "The system did not grant this" and kept **Continue** disabled | `PermissionNotifier.checkInitialPermissions()` runs only in the constructor; never re-read on resume | **P1** | OPEN | Re-read on `AppLifecycleState.resumed`; add a "refresh" affordance | User is stuck on the permissions screen until force-quit |
| 9 | Pre-auth consent sync | Silent/queued | Red **"Missing or invalid authorization header"** on every consent row during onboarding | `POST /api/v1/consent` → 401 before the user exists, and the raw message is rendered | **P1** | OPEN | Queue consent locally and sync post-auth, or suppress the technical message and show calm copy | Alarming, technical, and the first thing a new user sees |
| 10 | First-run order | Authentication first | **FIXED & VERIFIED ON DEVICE 2026-09-23.** A genuine uninstall/reinstall (`pm clear` is refused to `shell` on this ColorOS build) opened the *Meet NOVA* welcome page. Now a fresh install opens **Welcome back** with country + phone and a single CTA, *Continue with OTP*; no company/campaign/customization/language/dashboard screen is reachable, and email+password is behind an explicit switch. After the OTP the app lands on onboarding. | Router gated onboarding before auth (`_entryLocation` returned `resumeStep()` for any incomplete status) | **P0** | **FIXED** — the auth gate now runs first and `/onboarding/otp` counts as an auth route; splash sends a signed-out user to `/login` | — | A brand-new user was personalised before they had an account, and the product's specified first screen did not exist |
| 11 | Phone + Firebase OTP login (client + server) | Primary login, end to end | **WORKS, VERIFIED ON DEVICE 2026-09-23.** Fresh install → `7868002606` → *Continue with OTP* → 6-digit screen reading `+91 XXXXXX 2606` → `123456` → `FirebaseAuth: Notifying id token listeners about user ( vZnCX8gd3RNxUjA46pDItGa9zdc2 )` → a **new `sessions` row `71afc016-c5cb-4346-b3b8-676e047e05a3` at 2026-09-22T13:19:58Z** → onboarding. | Fixed in this session: `firebase_auth` added, debug SHA registered, `/api/v1/auth/firebase/exchange` implemented, Firebase resolved lazily | **P0** | **FIXED** | — | This is now the live login path |
| 11a | Phone-auth app verification (Firebase Android SDK) | OTP request always reaches a code | **INTERMITTENT.** One run completed in ~2 min; the next opened a browser Custom Tab at `…gging.firebaseapp.com` showing *"Unable to process request due to missing initial state"* and the sign-in failed. | `Failed to initialize reCAPTCHA config: No Recaptcha Enterprise siteKey configured for tenant/project *`. `recaptchaenterprise.googleapis.com` is **disabled** in the project; the SDK falls back to the reCAPTCHA web flow, which is broken. The service account has no `serviceusage.services.enable`. | **P0** | **BLOCKED** | See §Blocked below — a project Owner must enable the API and provision the reCAPTCHA/Play Integrity config for `com.leadup.nova` | Users are intermittently dumped into a broken browser page instead of receiving a code |
| 11b | OTP send count | One SMS per sign-in | **FIXED & VERIFIED.** The login screen sent a code, waited for `codeSent`, routed to the step, and the step **sent a second one** for the same sign-in. | The `/onboarding/otp` builder supplied the transport but not `requestOnStart`, which defaults to `true` | **P1** | **FIXED** — the route passes `requestOnStart: false` when the caller supplied a transport | — | Double SMS cost and needless pressure on the per-number throttle |
| 13 | Permission caption on a fresh install | The row states the system's real answer | Both Microphone and Notifications read **"The system did not grant this. You can allow it later."** before the user had tapped anything | `permission_handler` reports `denied` for *never asked* as well as for *refused*, and the caption treated one as the other | **P1** | **FIXED** — a row that has not been asked says "Not turned on yet"; a real refusal still reads as one. Verified on device (`perm_fixed.png`) | — | A false failure on the first screen after login; users were told the system refused something it was never asked for |
| 14 | Notification permission request | Button raises the OS dialog | Previous round recorded **no dialog across three taps** | The earlier observation was a transient device state, not a broken path — this round the dialog appeared on the first tap and `POST_NOTIFICATIONS: granted=true` | **P1 → RETRACTED** | **NOT A DEFECT** — re-tested and granted normally | Re-check if it recurs on a clean device | — |
| 15 | Top bar vs floating avatar | Avatar prominent without covering controls | The 80px orb sat through the notification bell and settings icon on Home, and through the sign-out button on Profile. Excluding Home alone was not enough. | The overlay hardcoded `top: 60`, which is inside `NovaScaffold`'s top bar (`topInset` 44 + a 44px control row + 16px padding, ending at 104) | **P2** | **FIXED** — the orb is not drawn on `/`, and everywhere else it sits at `_kOrbTopBelowTopBar` (112), below the shared top bar. Both screens re-verified on the handset (`home_fixed.png`, `profile_orb.png`) | — | A control the user needs was covered by decoration on two screens |
| 12 | Language selection | All genuinely supported Indian languages | **FIXED & VERIFIED ON DEVICE 2026-09-23.** The picker now lists 27 options — `auto`, all 22 catalogue languages and the 4 code-switching styles — and the sheet showed **Telugu selected** after it had been persisted through the API. | Four hardcoded chips in two files, a four-value API enum that existed **twice** (`routes/settings.ts` + `schemas/index.ts`), and a client allowlist `{en,ta,hi,auto}` that rewrote every other choice to `auto` | **P1** | **FIXED** — one shared catalogue-derived `LanguagePolicySchema`, one shared `languagePolicyOptions()`, and `kVoiceProtocolLanguages` derived from `kSupportedLanguages` | Mark any language whose voice is an English fallback as partial in the picker | Non-Tamil Indian users could not choose their language, and a choice made on the server was discarded on the wire |
| 13 | Urdu/Nepali/Kashmiri/Bhojpuri/Awadhi voice | Spoken in-language | TTS provider reports `elevenlabs-fallback` — an English voice reading non-Latin script | Google provider selected in the catalogue but no Google key is configured | **P2** | OPEN | Either configure Google TTS or remove these from the selectable list and label them partial | Replies are textually right, spoken wrong |
| 14 | Avatar prominence | Large, centred, non-overlapping | Bubble overlaps the header bell and settings gear on every screen | In-app overlay positioned without reserving layout space | **P1** | OPEN | Move the bubble clear of the app bar or collapse header actions while it is shown; enlarge the Home avatar | Controls are visually and possibly physically blocked |
| 15 | Task/reminder state transitions | create/modify/complete/reopen/snooze/cancel | Serialized by the tool layer and covered by the API suite; **create** verified end-to-end this session | — | **P2** | OPEN | Re-verify each transition acoustically once §5/§6 are fixed | Lower risk than the above |
| 16 | Multi-turn context ("that", "it") | Resolve references | Not verified this session | — | **P2** | OPEN | Add a device test for pronoun resolution | Unknown |
| 17 | Company/business configuration | Collected in onboarding | No such screen in the observed flow | Not implemented | **P2** | OPEN | Add the step if the product requires it | Enterprise personalisation missing |
| 18 | iOS | Buildable with Firebase | `apps/mobile/ios/Runner/GoogleService-Info.plist` is a placeholder and not in `project.pbxproj` | Never wired | **P3** | OPEN | Add the real plist to the target | iOS build has no Firebase |

## Cross-cutting notes

- **The assistant brain is ahead of the shell.** STT, the language-aware prompt, the tool
  layer, the reminder scheduler and the audit trail all behaved correctly once the two
  guards were widened. The failures that block daily use are transport (proactivity),
  process lifecycle (foreground service / wake word / Doze) and first-run flow.
- **Two guards were the same bug in different languages.** Both the invented-time guard
  and the false-claim guard were correct for English and Tamil and blind everywhere else.
  Any future guard that pattern-matches user or model language should be treated as
  language-scoped until proven otherwise, and tested in at least the tier-A languages.
- **Do not read a green reply as a green action.** The Telugu failure produced a
  perfectly-scripted Telugu sentence while nothing was created; only the reminder count
  revealed it. Outcome assertions, not reply assertions, are what caught it.

| 16 | Device registration vs version skew | A newer app can still register | The app reported `pushToken` and the endpoint was `.strict()`, so against a server one version behind **the whole registration was refused**: `400 Unrecognized key(s) in object: 'pushToken'`. Since registration is the only writer of `devices.push_token`, a client that cannot register can never receive a proactive nudge. | `.strict()` on a client-reporting endpoint | **P1** | **FIXED** — unknown keys are ignored, declared fields are still validated, and a bad value is still a 400 (both pinned by tests) | **Deploy the API to production** — push does not work there at all until then | Push silently breaks for every client newer than the server |
| 17 | Black screen after approving a tool action | The app stays usable | **FIXED & VERIFIED ON DEVICE 2026-09-23.** Approving left the routed tree empty — go_router renders `SizedBox.shrink()` for an empty match list, which over the window's own background is pure `#000000` with no chrome and no way out but a force-stop. A route trace named the cause in one run: `POP ModalBottomSheetRoute prev=converse`, then `POP converse prev=-` **3 ms later**, then `delegate matches=0`. | `_maybePromptVoiceApproval`'s "the request went away, take the sheet with it" branch closed the sheet with `Navigator.maybePop()`, but **approving clears `pendingApproval` too** — the same signal that branch watches — and `_showingVoiceApproval` is not cleared until the suspended call's `finally` runs. So the normal approve path popped the page as well. Intermittent because it raced that `finally`. | **P1** | **FIXED** — `_decidingApproval` brackets the state change the decision causes, so the branch stands down while a decision is being applied and still works for a request that genuinely vanishes. Verified twice on the handset: one pop, no `matches=0`, screen rendering. | — | A user who approved an action could be left with an unusable app |

| 18 | MacBook acoustic loop, both directions | Phone hears the MacBook; the MacBook captures what the phone says | **VERIFIED 2026-09-23.** Room floor RMS 0.0050; the question from the MacBook's speakers 0.0334; then a speech-shaped burst at **0.0176 / 0.0137** across 18–26 s, bracketed by silence, which is NOVA's voice arriving at the MacBook's microphone. The reply content was grounded and correct. | — | — | **VERIFIED** | — | The mandate's §11 loop is demonstrated end to end |
| 19 | Task lifecycle (§13) — create / modify / reschedule | The assistant can change a task it already made | **FIXED & VERIFIED 2026-09-23 — three defects found and closed.** (1) **Modify and reschedule had no tool at all**: tasks offered only create/complete/reopen, so "move that to Friday" was answered with "I need the time for Friday" and nothing moved. `update_task` now exists. (2) `create_task`'s description told the model to *ask* which hour when given a day, so "create a task to file the claim on Thursday" created nothing and the follow-up lost the thread; a task's deadline is optional, so a bare day is now filed and used. (3) A placeholder id (`"task_id_for_warranty_claim"`) passed `z.string()` and reached Postgres, which raised `invalid input syntax for type uuid`; ids are now `z.string().uuid()`. | See each item | **P1** | **FIXED** | — | The middle of the promised lifecycle did not exist |
| 19a | Reopen can act on the wrong task | The named task is the one changed | **FIXED & VERIFIED 2026-09-23.** Two causes, both removed. (1) The context query filtered to `['pending','in_progress']`, so a **completed task was invisible** and the model had no valid id — it answered *"I don't have the task ID … in my list"*, or bound a *pending* task's id and reopened the wrong record. Recently-completed tasks are now included (bounded to 5, own heading, labelled `completed`). (2) `setTaskStatus` wrote unconditionally, so reopening an already-pending task reported `ok` for whatever row it was handed. A status change that reverses nothing now fails and **names the task it received**. Verified live: create → move → complete → reopen all land correctly, and the wrong target can no longer be silent. | See above | **P1** | **FIXED** | — | A wrong record was mutated while the user was told the right one changed |
| 19b | Reply does not acknowledge the action | The reply names what was done | Seen once: on `complete_task:ok` the reply was *"Got it. I'll treat execution logs and tool results as actual operations performed."* | Model prose, not the tool | **P3** | OPEN — **not seen again** in three later runs, each of which named the task and the action | — | Confusing, but not misleading about the outcome |
| 19c | Reminder lifecycle: snooze, recurring, cancel, overdue | §14's list | **VERIFIED 2026-09-23** against the live assistant, each step read back from the database: create → *"Reminder set for tomorrow at six in the evening"* (`Thu 24 Sep 18:00 IST`); modify → *"now set for tomorrow at seven"* (`19:00 IST`); snooze (`in_minutes`) → moved to now+30 min; repeat → `repeatRule = FREQ=DAILY`; cancel → `dismissed = true`. Overdue was checked too: *"Do I have anything overdue right now?"* correctly named two reminders with relative times. | — | — | **PASS** | — | §14's reminder lifecycle is demonstrated |
| 19d | Morning briefing (§16) | A grounded summary of the day | **VERIFIED 2026-09-23.** `GET /briefing` returned `source: grounded` with real counts (`missedReminders: 2`, `upcomingReminders: 3`) and an honest `capabilities` block (`calendar:false`, `weather:false`). The anti-fabrication guard had fired on the model's own draft (`guardRejection: ungrounded-quantity:one`) and the reply fell back to grounded text. | — | — | **PASS** | — | The proactive briefing works and refuses to invent numbers |
| 19e | Snooze semantics | "Snooze it by 30 minutes" shifts the reminder | **OBSERVATION, NOT A DEFECT.** Applied to a reminder due *tomorrow 7 PM*, the snooze moved it to **now + 30 minutes** — per the tool's documented contract (`in_minutes` is measured from the current time). A user saying "snooze" about a future reminder may mean "30 minutes later than scheduled". Worth a product decision, not a bug. | Documented semantics | **P3** | OPEN | Decide whether a post-fire snooze and a pre-fire relative move should share one verb | Could surprise a user, but the reply states the resulting time plainly |

| 20 | Four languages are spoken by an English voice | A chosen language is spoken in that language, or the product says it cannot | **MEASURED & PARTIALLY FIXED 2026-09-23.** Reading the `provider` field of `/voice/tts`: `hi → sarvam`, `ks → sarvam-fallback`, and **`ur`, `ne`, `bho`, `awa` → `elevenlabs-fallback`** — an English voice reading the correct script. The catalogue named Sarvam for those four. | `voiceProvider` records the provider that is *tried*; the fallback chain (Sarvam → Deepgram → Sarvam fallback → ElevenLabs) returns audio for a language no provider serves | **P1** | **PARTIALLY FIXED** — `voiceFallback` now marks the four codes and the picker reads *"Urdu (basic voice)"*; both suites pin the set. No Indian voice has been found for them, so they are labelled rather than fixed | Flag any language whose voice is a fallback, anywhere it is offered | Presenting them as plain choices is exactly the mislabelling the mandate forbids |

| 21 | Task lifecycle by voice on the handset | Create / modify / reschedule / complete / reopen / history, spoken | **VERIFIED 2026-09-23.** Four spoken turns, each approved in the sheet and confirmed against the API: *"create a task to renew the domain in Thursday"* → `create_task:ok`, pending due Thu 24 Sep; *"move that task to Friday"* → `update_task:ok`, due Fri 25 Sep 00:00 IST; *"mark the domain task as complete"* → `complete_task:ok`, completed; *"actually reopen it"* → `reopen_task:ok`, **pending**. `/activity` recorded `task.create · task.update · task.complete · task.reopen`, all success. | — | — | **PASS** | — | §13 is demonstrated end to end through speech, which three earlier rounds could not do |

| 22 | A voice provider the catalogue cannot reach | Every routed provider has a credential, or the deployment says so | **DIAGNOSED & SIGNALLED 2026-09-23.** `GOOGLE_CLOUD_API_KEY` is **not set** while Sarvam, ElevenLabs and Deepgram are, and the catalogue routes **seven** languages to Google (`as, mai, sa, sd, ks, doi, mni`). Proven directly: `google refused ur -> GOOGLE_CLOUD_API_KEY is not configured`; `ks` (google-routed) came back `sarvam-fallback`. Nothing said so anywhere. | A missing credential is invisible: the provider throws, the TTS chain falls through, and the user hears a different voice | **P1** | **SIGNALLED** — the API logs one line at boot per unrouted provider, naming the env var and every affected language; a test pins that every catalogue provider has a credential mapping. Routing is deliberately unchanged, because the catalogue is right if the key is added. | **Provide `GOOGLE_CLOUD_API_KEY`** — an operator credential | A third of the language catalogue was degrading silently |

| 23 | Duplicate, conflicting and nonsense commands (§23) | No duplicate work, no crash | **VERIFIED 2026-09-23** against the real assistant. The same reminder asked twice in one conversation produced **one** row and NOVA said so outright (*"You already have a reminder to call the bank tomorrow at five o'clock…"*). Two different reminders at the **same instant** were both kept, and the second turn noticed the first. Nonsense input (*"asdfgh qwerty zxcvbnm"*) got a graceful reply and created nothing. | — | — | **PASS** | — | Double reminders and junk input are handled |
| 24 | Network failure is silent | The user is told the server is unreachable | **FIXED & VERIFIED 2026-09-23.** With the API stopped and the device pointed at a dead port, the app sat on **"Thinking…" for over 40 seconds** with an empty bubble, nothing in `logcat`, and no explanation. | `_ensureConnected` bounded only its wait for a status event, not the `await _service.connect()` above it — so a connect that never returned hung the method and the timeout was never reached, and the `offline` message the controller already has was never raised | **P1** | **FIXED** — one `_connectDeadline` now covers the call and the wait. Re-verified on the same dead port: status **ERROR**, *"The voice connection dropped. Reconnecting…"*, avatar on "Something went wrong", composer usable. | — | A slow answer and a dead server looked identical |
| 25 | Latency (§24) | Measured, with the bottleneck identified | **PARTIALLY MEASURED 2026-09-23.** Cold launch **1781 ms** (debug/JIT build, so an upper bound). Per spoken turn, from the API's own `Realtime turn timings`: **context build 11–15 ms**, **model 2778–3864 ms**, **first token 2112–2302 ms**. End to end from speech end to reply start: **~4.5 s**, from the acoustic recording. | — | — | **MEASURED** — the model dominates; the context layer is negligible | TTS-to-first-audio, STT-after-speech and reminder-trigger latency are unmeasured | The user waits ~2.2 s before anything appears and ~4.5 s for the reply |
| 26 | Language switch within a conversation | The reply matches what the user is speaking; a pinned language holds | **VERIFIED 2026-09-23.** Three turns in one conversation, twice, counting the reply's dominant script. With `auto`: English → Latin, **Hindi → Devanagari**, English → Latin — it follows the speaker mid-conversation and back. With the policy pinned to `hi`: all three answered in Devanagari, so **the pin outranks the spoken language**, which is the right reading of a standing setting but worth stating. | — | — | **PASS** | — | §9's same-language rule holds across a switch |

| 27 | Reminder with the screen locked (§14) | A reminder fires on a locked, screen-off phone and is visible | **VERIFIED 2026-09-23.** A reminder set two minutes out for 02:51:21 IST, the app backgrounded and resumed twice to arm it, then the screen turned off and locked (`mScreenOn=false`). It fired anyway: `dumpsys notification` shows `id=185948139 channel=nova_reminders_v2 importance=4` with the screen still off, and the **lock screen** shows *"NOVA · now · NOVA reminder · Locked screen check"* in the alerting group. | — | — | **PASS** | Still untested: app killed, device restart, network interruption at the fire time | Reminders work when the phone is in a pocket |

| 28 | Reminder with the app process killed (§17) | The reminder still fires | **VERIFIED 2026-09-23.** Reminder set for 02:57:06 IST, app backgrounded and resumed to arm it, then `am kill` at 02:54:36 — `pidof` returned nothing. It fired anyway: notification `id=570763834 channel=nova_reminders_v2 importance=4`, text *"Killed app check"*, and **`pidof` returned 16750** afterwards, so Android restarted the app to deliver it. `dumpsys alarm` had shown 12 `ScheduledNotificationReceiver` entries while the process was dead. | — | — | **PASS** | **`force-stop` is a separate case and cannot work**: it puts the app in a stopped state and cancels its alarms, by platform rule. Stated in the report rather than left implied. | Reminders survive the system reclaiming the process |

| 29 | Reminder across a device restart (§14/§17) | The reminder still fires | **VERIFIED 2026-09-23.** Reminder due 03:10:04 IST, phone rebooted at 03:03:34. `dumpsys alarm` read **0** for the app at boot+55 s and **18** again by 03:11:31, and the notification (text *"Reboot survival check"*) was present by 03:14:58. `ScheduledNotificationBootReceiver` re-registers the schedule because reminders are armed through the plugin's `zonedSchedule`. | Android clears alarms on reboot; the plugin's boot receiver restores them | — | **PASS, with a latency caveat** — delivery arrived somewhere in the **87 s–294 s** window after the target, consistent with the inexact-alarm finding in row 4f, worsened by post-boot load | **Exact-alarm delivery**: a 6 PM reminder can arrive minutes late | Reminders survive a restart, but not punctually |
| 30 | Microphone denied (§23) | The app says so and recovers | **VERIFIED 2026-09-23.** Mic revoked through Settings (all three adb routes are refused by ColorOS: `pm revoke`, `appops set`, `cmd appops set`). Tapping the mic raised Android's own dialog (*"Allow NOVA to record audio?"*); choosing **Don't allow** set `USER_FIXED` and the app showed **"Microphone access is blocked. Turn it on for NOVA in Settings."**, returned to **READY** rather than sitting in *Listening*, stayed usable, and did not pretend to hear. Re-granting took effect on the next tap with no restart. | Android will not re-prompt once a permission is `USER_FIXED`, so pointing at Settings is the only actionable message | — | **PASS** | — | A revoked mic named itself instead of failing silently |
| 31 | A reschedule confirms the wrong time | The confirmation names the time that was stored | **ADDRESSED 2026-09-23, EVIDENCE LIMITED.** Measured: *"remind me again in 30 minutes"* on an already-fired reminder stored **03:54** (correct) but the reply said *"twenty to four"* (**03:40**). The tool had returned `trigger_at_local: "3:54 am"` in its `summary` and `data`; the prompt only said to "repeat the resolved date and time", which permits re-deriving it. | The time is computed in the tool correctly and re-derived in the model's prose incorrectly | **P2** | **CHANGED** — the prompt now says to copy the time from the tool result exactly and not to convert, round or recompute it. **2 of 2 post-change runs matched** (stored 04:04, spoken *"four oh four"*) and both actions moved. **This is supporting evidence, not proof**: no controlled before/after was run and prompt behaviour is stochastic. | A larger sample, or a structured confirmation rendered from the tool result | A time-critical promise stated wrongly in the sentence the user hears |
| 32 | Notifications denied (§23) | The app says what is lost | **VERIFIED 2026-09-23** (permission changed through Settings; all adb routes refused). With `POST_NOTIFICATIONS: granted=false` the app asks on launch, and once the reconcile pass runs the **Status** card shows **"Reminders cannot reach you"** — *"Android is blocking NOVA's notifications, so a reminder is armed but never shown. Nothing is lost — …"*. Granting again clears it. | The app must not keep arming reminders the user can never see without saying so | — | **PASS**, with two open sub-questions | **Sub-questions resolved from the reconciler source** (see §37): `osBlocked` is read live from the OS on every reconcile and passed into every return path, so the card is not transient while the permission is denied; the capture that appeared to lack it was at the top of Home and did not show the Status section at all. **The two-sample device confirmation was attempted twice and both times the notification toggle did not change state** (`dumpsys` still `granted=true`), so it remains reasoning-from-code plus one valid observation rather than a device result. | A user who silently loses reminders has no way to know |
| 33 | Driving Settings by fixed coordinates | A restore step leaves the device as found | **OCCURRED & FIXED 2026-09-23.** While restoring the notification permission, a tap meant for **Allow notifications** hit **Ringtone** instead — the two switches share a screen — silently turning the reminder channel silent, which is the §16 defect returning. Caught because the NOVA notification page read *Ringtone: off* and the Reminders category listed only *"Notification drawer, Banner, Lock screen, Vibrate"*; toggled back and it lists *Ringtone* again. | Two adjacent switches on one screen cannot be addressed safely by fixed coordinates | **P3** | **FIXED** — and now verified after every settings interaction | Verify device state after any blind-tap sequence | A silent reminder is exactly the failure the §16 work removed |
| 34 | App killed mid-turn (§23) | Nothing is lost and nothing is duplicated | **VERIFIED 2026-09-23.** A dictated task was approved, the app backgrounded one second later and killed (`pidof` empty), and the server finished the turn it was mid-way through. Result: the row exists (**"mid-fly check to call the vendor"**, `pending`, due Thu 24 Sep 00:00 IST — the time the sheet promised), **exactly one** row against zero before, no duplicate, and the relaunched app came back **READY** with no `FATAL`/`ANR` for the package. | The write is executed server-side, so killing the client cannot split it | — | **PASS** | — | A client killed mid-action neither loses nor duplicates the write |
| 35 | A reminder due while offline (§14) | It fires without connectivity | **VERIFIED 2026-09-23.** Reminder set for 03:40:55 IST with 30 alarms armed, then airplane mode enabled and confirmed (`airplane_mode_on: 1`, *"Network is unreachable"*) before the target. It fired anyway: the notification **"Offline fire check"** was present by 03:43:55, absent at 03:41:51. | The alarm belongs to `AlarmManager` and the notification is scheduled on-device, so delivery never touches the API | — | **PASS** | Delivery landed in the **56–180 s** window after the target — wider than the ~84 s window measured online, plausibly because airplane mode deepens Doze. One sample; no rate claimed. | A reminder set before a flight still arrives |
| 36 | Reminder punctuality | A reminder fires at the time asked for | **FIXED & VERIFIED END TO END 2026-09-23.** Reminders were armed with windows of 22–40 minutes. Instrumented on the handset: the permission probe said `exact=true`, the app requested `exactAllowWhileIdle`, and the plugin **returned without throwing** — the mode was accepted and then batched below the Dart layer. Matched alarm-to-reminder by `origWhen`: `exactAllowWhileIdle` → `windowLength 1839803` (~31 min) against **`alarmClock` → `windowLength 0`**. | This OEM does not honour the `exact*` modes the plugin requests, but does honour `setAlarmClock` | **P2** | **FIXED** — `AndroidScheduleMode.alarmClock` on the one-shot, repeating and briefing paths, with a windowed fallback on the one-shot path. Post-fix: the specific alarm reads `windowLength 0`, **13** NOVA alarms are exact where there were none, and a reminder due at **04:30:26 fired at 04:30:28 — 2 s late**, against 87–294 s before (§46). **The grant is still required** — with it off the same measurement returns `windowLength 1614977`, so the Reminders notice asking for it is correct (round 42). | — | A time-specific reminder now fires at the time asked |
| 37 | ~~The exact-alarm notice is now unnecessary~~ | — | **RETRACTED 2026-09-23.** Created on a source reading that `alarmClock` skips the plugin's `canScheduleExactAlarms()` check. Measured instead: with the *Alarms & reminders* toggle **off**, the alarm returned `windowLength 1614977` (~27 min) and the app held **zero** exact alarms; with it on, the same reminder reads `windowLength 0`. The notice is necessary. | — | — | **RETRACTED — no change needed.** The code comment making the opposite claim was corrected. | — | — |
| 38 | Same request occasionally answers instead of acting | The request behaves predictably | **MEASURED AND RE-ATTRIBUTED 2026-09-23.** Four clean-or-cleaner sweeps of the identical request across all 13 required languages gave 9, 10, 9 and **10/13** acting, with 13/13 same-language replies every time. The three languages that did not act in the clean sweep were then re-sampled from a clean state: **Hindi 2/2 acted, Bengali 2/2 acted, Odia 1/2**. So the behaviour is **run-to-run variance, not language-dependent** — the earlier "depends on the language" reading was n=1 noise. | Model sampling, not language routing | **P3** | **OPEN as a consistency observation** — the multilingual claim is withdrawn. Roughly 10–12 runs in 13 file the task; the rest ask a clarifying question. | Only worth changing if a higher rate is wanted; any such change needs many samples per language to demonstrate | A user cannot predict whether the request files the task or asks a question |
| 39 | Urdu and Nepali spoken by an English voice | A chosen language is spoken in that language | **FIXED & VERIFIED 2026-09-23.** The cause was the model: `synthesizeSpeech` hardcoded `eleven_flash_v2_5` and sent no `language_code`, and Flash has no voice for `ur` or `ne` — it rejects the code outright (*"does not support language_code 'ur'"*). `eleven_v3` covers both. Only languages Flash cannot speak are routed to v3, with the language code. | The catalogue had blamed Sarvam routing; the provider was never the problem, the model was | **P1** | **FIXED** — round-tripped through Deepgram: the mangled word (*आईंड* for *کلائنٹ*) is now intelligible, and the served audio is byte-identical to a direct `eleven_v3` call (36 824 bytes vs Flash's 39 750). **Follow-up:** the picker still says "(basic voice)" for these two and should be revisited after a listening check. | Un-label them for `ur`/`ne` once heard | Two languages were read aloud in the wrong language entirely |
| 40 | Bhojpuri and Awadhi have no voice | A chosen language is spoken in that language, or the product says it cannot | **BLOCKED — CONFIRMED BY ALL THREE PROVIDERS 2026-09-23.** Sarvam `bulbul:v3` validates `target_language_code` against a fixed list and prints it on rejection; `bho-IN` and `awa-IN` are **not in that list** (not merely gated). ElevenLabs has them in neither `eleven_flash_v2_5` (32 languages) nor `eleven_v3` (74). | No configured provider offers a Bhojpuri or Awadhi voice | **P1** | **BLOCKED** — they stay labelled "(basic voice)" in the picker, which is honest but means the language is not actually spoken. **Required:** a TTS provider with Bhojpuri and Awadhi voices, or a decision to offer them as text-only. | Do not offer them as spoken languages until a voice exists | A user who picks Bhojpuri hears a voice that is not Bhojpuri |
| 41 | Native Indic voices available on request | The best available voice is used | **IDENTIFIED 2026-09-23.** Sarvam `bulbul:v3` lists `ur-IN`, `ne-IN` and `ks-IN` but refuses them with *"Please request beta access to `<code>` by contacting our support team."* Urdu and Nepali therefore run on ElevenLabs' generic multilingual model, and Kashmiri on a fallback voice, when a native Indic voice exists behind an access request. | Account access, not code | **P2** | **BLOCKED — operator action.** **Required:** request beta access to `ur-IN`, `ne-IN` and **the seven in row 49** (`as-IN`, `mai-IN`, `sa-IN`, `sd-IN`, `ks-IN`, `doi-IN`, `mni-IN`) from Sarvam, then re-measure the provider each language is served by. | Request the beta access, then drop the ElevenLabs routing for these three | Two languages are spoken by a generic model when a native one is one email away |
| 42 | Urdu voice input on the upload path | Speech in a supported language is understood | **FIXED & VERIFIED 2026-09-23, SCOPE CORRECTED.** `transcribeAudio` accepted a `language` argument and never used it, so the Deepgram request ran the English default: the same Urdu clip returned **`''`** as shipped and *"कल सौ दस बजे client को call करें."* with `detect_language=true`. This is the **REST/upload** path (Sarvam `saarika:v2.5` refuses `ur-IN`). **The live conversation path was never affected** — Sarvam's streaming model accepts `ur-IN` and returns *"کل صبح دس بجے کلائنٹ کو کال"*. | The argument was accepted and silently dropped | **P1** | **FIXED** on the upload path; live path verified working. **My earlier recommendation to set `sttProvider: deepgram` for `ur`/`ne` is WITHDRAWN** — one field feeds both paths, and that change would move a verified-good streaming language onto an unverified model. | If the wasted REST call matters, fix it in `transcribeAudioForLanguage`, not in the catalogue | Uploaded Urdu audio was discarded |
| 43 | Nepali voice input on the upload path | Speech in a supported language is understood | **FIXED & VERIFIED 2026-09-23.** After the row-42 fix the path returned gibberish (*"Hana das Bagegra Hacklai Phone Gardnose."*) because detection alone is not enough: `nova-2` carries no Indic language and the language must be *stated*. `nova-3` + `language=ne` returns **"भोलि बिहान १० बजे ग्राहकलाई फोन गर्नुहोस्"** — correct. Non-English now asks for `nova-3` with the code, degrading to detection only if Deepgram refuses the code. | Two things were wrong: the model, and the missing language | **P1** | **FIXED** — verified through `transcribeAudioForLanguage`: Nepali correct, Urdu correct (*"کل صبح 10 بجے کلائنٹ کو کال کرے۔"*), Hindi unchanged on Sarvam | — | Uploaded Nepali audio was transcribed into gibberish |
| 44 | The MacBook loop in an Indian language | A spoken Indian-language exchange works end to end | **VERIFIED 2026-09-23.** MacBook speakers (macOS Hindi voice *Lekha*) → phone mic → Sarvam streaming STT → model → TTS → phone speaker → MacBook mic, recorded at 16 kHz. The phone transcribed *"आज मेरे पास क्या क्या काम है?"* exactly, and answered **in Hindi, in Devanagari, grounded in this account's real reminders** (*"क्लायंट को कॉल करना — सुबह नौ बजे…"*). Return path measured: quiet floor ~124, **peak ~2116 (17×)**, a ~15 s speech burst starting after the MacBook stopped. | — | — | **PASS** | — | The full §11 loop works in an Indian language, not only English |
| 45 | The pinned language never reached the microphone | Voice runs in the language the user selected | **FIXED & VERIFIED 2026-09-23.** Every voice entry point read the persona with `ref.read(personaProvider).asData?.value.languagePolicy`. `personaProvider` was a lazy `FutureProvider.autoDispose`, so that **read returned null** on the first turn of every app run and `normalizeVoiceLanguage(null)` answered `'auto'` — the user's choice was discarded. Measured: with the persona pinned to `hinglish` the session logged `"language":"auto"`, and no `/settings/persona` request appears in the log at all. | A lazy provider read without awaiting is null, and null normalises to auto by design | **P1** | **FIXED** — the provider is no longer `autoDispose` and all four call sites await `personaProvider.future`. Verified: the session now logs `"language":"hinglish"`, and the reply changed from *"I'm not quite sure… Portuguese…"* to a correct grounded Hinglish answer. | — | **The language picker did not affect the microphone**, so a user who chose an Indian language could be recognised as English |
| 46 | Romanised Hindi speech transcribes imperfectly | Speech is transcribed as spoken | **MEASURED 2026-09-23.** *"Kal subah mera kya schedule hai"* spoken by an English voice transcribed as *"कै सब अमेरिका ये स्केजुल हाई बात है ना प्लीज।"* The meaning survived — the assistant answered correctly — but the words did not. | Romanised Hindi spoken with English phonetics is a hard case for every available recogniser; no provider setting was found that fixes it | **P3** | **OPEN** — quality limitation, not a blocker | Accept as a known limit, or evaluate a code-switch-aware model | Interpreting a mixed sentence may fail on unusual phrasing |
| 47 | Tamil, spoken, on the handset | A spoken Tamil exchange works end to end | **VERIFIED 2026-09-23.** With the persona pinned to Tamil, the session logged `"language":"ta"` (the fix in row 45 holding on a second language), the transcript of *"நாளை எனக்கு என்ன வேலை இருக்கு?"* was **exact**, and the reply came back in Tamil script, colloquial, grounded in the account's real reminders (*"5 மணிக்கு மருந்து கடை ஆர்டர் எடுக்க வேண்டும்…"*). Return path: floor ~133, peak 1618, 17.8 s of speech. | — | — | **PASS** | — | §7/§9 demonstrated for Tamil with the physical rig |
| 48 | The pinned-language fix could have regressed a language | Sending the chosen code never makes a language worse | **CHECKED 2026-09-23.** Round 55 changed the voice session from `auto` to the pinned code, which would be a regression if any provider **rejected** a language it had been handed as `auto`. Every code the app can send was opened against the streaming endpoint — `auto`, the 12 required languages, `od-IN`/`or-IN` both, `ur-IN`, `ne-IN` — and **15/15 are accepted**. | — | — | **PASS — no regression** | — | The fix cannot make a language worse by being rejected |
| 49 | Seven languages are requested in English | The voice asked for is the language being spoken | **MEASURED 2026-09-23.** `as`, `mai`, `sa`, `sd`, `ks`, `doi` and `mni` are routed to Google (unconfigured, row 22), fail over to the Sarvam fallback, and `toSarvamCode` has **no entry for any of them** — its default is `en-IN`. So Assamese goes out as `target_language_code: "en-IN"`. Proven by size: the app's Assamese audio is **158 098 bytes**, identical to a direct `en-IN` request, against `bn-IN`'s 150 572. | The fallback's language map covers 14 languages and defaults to English for the rest | **P2** | **OPEN — and not simply correctable.** Sarvam lists all seven native codes and **refuses every one** with *"Please request beta access"*, so mapping them would return 400 and leave those languages with **no audio**. **Required:** the beta access in row 41, extended to these seven. | Keep the accidental fallback until a native code is available; do not "fix" the mapping | The provider and language named in the request are both wrong, even though the audio is broadly the right language |
| 50 | Admin app-bar title truncates | The screen names itself | **FIXED & VERIFIED 2026-09-23.** The title rendered as **"Admin C…"**. Root cause: the bar reproduced CSS `space-between` with two `Spacer`s plus a `Flexible` title, so the spacers (which are `Expanded`) took two thirds of the free width. Replaced with `mainAxisAlignment: MainAxisAlignment.spaceBetween`, which distributes the gaps and leaves the title whole. | Reproduced `space-between` with spacers instead of the alignment | **P3** | **FIXED** — `r61_admin.png` shows "Admin Console" in full | — | A screen whose own name is cut off reads as unpolished |
| 51 | Offline copy reads as a contradiction | The screen agrees with itself | **FIXED & VERIFIED 2026-09-23.** The headline said *"Your saved reminders still work"* above a row reading **"Reminders — Not cached"**. Reworded to *"Reminders you have already set still fire"*, which names the distinction: an armed alarm fires offline (verified with the radio off), the list is not cached. | Two statements written for different purposes placed adjacent | **P3** | **FIXED** — `r61_offline.png` captured with airplane mode on; the widget test now asserts the reassurance, not the phrasing | — | A user could not tell whether their reminders would fire |
| 52 | Translate buttons stack one per row | Actions sit side by side as a row of controls | **FIXED & VERIFIED 2026-09-23.** The buttons rendered one per line (`Speak` above `Paste`; `Copy`/`Edit`/`Speak`/`Share` in four rows), wasting roughly four lines per card. Root cause: the actions are in a `Wrap`, but each child was `SizedBox(height: 44, child: Center(child: …))` and **a bare `Center` expands to the available width**, so every button claimed a whole line. Fixed with `Center(widthFactor: 1)`, which leaves the 44 px hit height intact. | `Center` without a width factor inside a `Wrap` | **P3** | **FIXED** — `r62_translate.png` shows both groups on single rows; a scan of every other `Wrap` in `lib/` found no second instance | — | A cramped, taller screen for no reason |
| 53 | "NOVA answers out loud" is false for typed messages | The promise matches the behaviour | **MEASURED 2026-09-23.** The Converse empty state says *"Tap the mic and just talk, or type a message. NOVA answers out loud as it thinks."* A typed message produced a text reply and **no audio**: MacBook mic floor ~144, peak ~236 (1.6×), with **no TTS request in the server log**. The control run had every credential valid and behaved identically, which is what rules out TTS failure as the cause. | The copy was written for the microphone path and covers the keyboard path too | **P3** | **OPEN** | Scope the sentence to the mic, or speak typed replies | A user who types expecting to hear a reply gets silence |
| 54 | The device-voice TTS fallback | Cloud TTS down -> the phone speaks with its own voice | **FIXED & VERIFIED 2026-09-23 — worked as designed.** The earlier block was that cloud TTS could not be failed without also failing STT; STT and TTS are separate modules, so a temporary env-gated stub at the top of `openSpeechStream` fails every cloud voice while recognition and the model stay real (reverted after the run; `git diff services/api/` empty). With all cloud voices down the server logged `TTS_ERROR`, the phone showed **"SPEAKING · ON-DEVICE"** and *"Cloud voice is unavailable — NOVA is speaking with the on-device voice"*, and the speaker produced a **17 s burst at 9.9× the noise floor**. | — | — | **PASS** | — | Cloud voice loss degrades to the device voice with an explicit notice, not to silence |
| 55 | `.env.test` was not gitignored | Environment files are never committable | **FIXED & VERIFIED 2026-09-23.** `services/api/.env.test` was untracked but **not ignored**. Its values are placeholders (`test`, `sk-tes…`) so nothing leaked, but it carries the key **names** and was one careless edit from being committed with real credentials. | `.gitignore` listed `.env`, `.env.local`, `.env.*.local` and `.env.production` individually, so any other `.env.<something>` slipped past | **P3 (security hygiene)** | **FIXED** — `.env.*` added with negations for the tracked templates. Verified: `.env.test` is now ignored and all six tracked `*.example` / `*.template` files remain unignored. | — | A filled-in test env file could have been committed silently |
| 56 | Avatar stuck on "Thinking…" after the turn completed | The phase matches what NOVA is doing | **ROOT CAUSE FOUND & FIXED 2026-09-23.** The server's two barge-in paths disagreed: a client-sent `cancel` ends the turn with a `done` frame, but the energy-detected `bargeIn()` ended it with only `speaking: false` — which the client reads as "the reply stopped, now thinking". So a barge-in whose audio carries no new utterance (a cough, a clip that ends) left the handset in `thinking` with the reply already on screen, until the user spoke again. | `bargeIn()` never sent the terminal frame its sibling path sent | **P2** | **FIXED** — `bargeIn()` now sends `done` with the partial reply, exactly as `cancel` does; the client commits the partial and returns to listening. The 50-second sighting was never reproduced on demand, and the one-turn stall is the mechanism this closes. The client-side `thinking` deadline contemplated earlier is not needed; the server now guarantees a terminal frame on every exit path | API suite re-verified green (1122 passed) | The avatar could misreport what NOVA was doing after an interrupted reply |
---

## Blocked — exact action required

These are not code problems and cannot be closed from this machine.

### B1. Firebase phone-auth app verification (blocks row 11a) — P0

**STATUS: BLOCKED**

**Reason.** The Firebase Android SDK must produce an app-verification attestation
before it will send an OTP. This project has neither provider configured:

- `recaptchaenterprise.googleapis.com` → `403 … has not been used in project
  nova-leadup-stagging before or is disabled`
- `androidcheck.googleapis.com` (legacy Android Device Verification) and
  `playintegrity.googleapis.com` → not enabled
- `GET identitytoolkit/v2/projects/nova-leadup-stagging/config` returns
  `recaptchaConfig: null`

Enabling them needs `serviceusage.services.enable`, which the Firebase Admin SDK
service account does not hold:

```
POST serviceusage.googleapis.com/v1/projects/nova-leadup-stagging/services/recaptchaenterprise.googleapis.com:enable
403 PERMISSION_DENIED  "Permission denied to enable service"
```

There is no `gcloud` on this machine and no application-default credentials, so
there is no broader credential to use.

**Required (project Owner, Google Cloud / Firebase console).** Any one of:

1. Enable **reCAPTCHA Enterprise API** for `nova-leadup-stagging`, then in
   Firebase Console → Authentication → Settings provision the reCAPTCHA key for
   the Android app `com.leadup.nova` (SHA-1
   `06:3F:DC:2F:0F:BD:4C:F7:0E:38:E7:1B:05:E6:70:52:A9:3F:68:C2`).
2. Or set up **Play Integrity** for the app and enable App Check.
3. Or grant the deploy service account `roles/serviceusage.serviceUsageAdmin`
   so the API enablement can be automated here.

**Already confirmed working without it.** The Firebase backend itself is correct:
phone provider enabled, and `+917868002606` is registered as a test number with
the fixed code `123456`. Server-side the full chain is proven —
`accounts:sendVerificationCode` → `accounts:signInWithPhoneNumber` (correct code
`200`, wrong code `400 INVALID_CODE`) → `POST /api/v1/auth/firebase/exchange`
`200` → `GET /api/v1/auth/me` `200` — and the on-device path completed once, with
a real session row as evidence. Only the SDK's attestation step is unreliable.

## Round 67 — audit additions (2026-09-24)

Findings from the codebase + live-server + CI audit. Status legend as above.

| # | Feature | Expected | Current (observed) | Root cause | Sev | Fix | Remaining work | Production impact |
|---|---|---|---|---|---|---|---|---|
| 57 | CI gate on main | PRs/pushes are gated | **GREEN FOR THE FIRST TIME 2026-09-25** (Backend CI, Mobile CI, Build Android APK, and the full CI/CD pipeline including E2E Tests all pass on main (migrations + three services booted against real containers, Playwright 3 passed/65 skipped). The last layers: Playwright's localhost→::1 resolution vs IPv4 services; tsx dev entrypoints resolving @nova/* to dist/ that CI never built (job now builds ...deps first); the health-wait loop never failed on timeout — it now exits 1). Fixed: pnpm version pins clashing with packageManager, test jobs missing env + turbo.json filtering it out, dead osv-scanner ref, dead Expo mobile.yml rewritten as Flutter CI, invalid maestro-e2e.yml and duplicate ci-cd.yml deleted, APK gate degrading to unsigned debug without the keystore secret, per-service builds missing workspace deps. **Historical measure:** Every push fails Backend CI (pnpm/action-setup version conflict vs `packageManager: pnpm@9.15.0`), CI/CD (`google/osv-scanner-action@v1` unresolvable; Unit Tests fail with `JWT_SECRET must be set to a strong value` — the job defines no env/services, so all 70 API suites can never boot there), Build APK (`ANDROID_KEYSTORE_BASE64` unset → fails instead of degrading), `mobile.yml` (dead Expo pipeline, zero jobs), `maestro-e2e.yml` (invalid YAML at line 78). | Workflow rot after the Flutter migration | **P0 (process)** | **FIXED 2026-09-25** | Watch next push; add branch protection only after green | Every merge to main is unverified by CI; the green suites exist only on a laptop |
| 58 | Auth rate limiting | Per-user brute-force protection | **FIXED & DEPLOYED 2026-09-25.** Was: 12 logins with 12 distinct XFF values 429'd at #11 (one global bucket). Now: `app.set('trust proxy', 'loopback')` in server.ts (only the loopback hop is trusted; client-injected XFF entries are ignored), nginx API blocks send `X-Forwarded-For $remote_addr`, three tests pin per-IP buckets + spoof resistance + no-XFF (suite 70/1125 green). Deployed to nova-leadup.in (`trust proxy` confirmed inside the running image); /healthz and a 401 login re-probed post-deploy. **Defect reproduced below for the record:** 12 POST /auth/login attempts with 12 distinct `X-Forwarded-For` values → 401×10 then **429×2**. `trust proxy` is never set (`server.ts`), so `req.ip` is the nginx socket address and the limiter is **one global 10/60s bucket for the entire product**. The 11th login anywhere in a minute is refused. | Missing `app.set('trust proxy', 'loopback')` | **P1 (availability)** | **FIXED & DEPLOYED 2026-09-25** | Re-measure after deploy | A modest traffic burst locks every user out of sign-in |
| 59 | Credential hygiene | No credentials in the tracked tree | **FIXED (repo side) 2026-09-25.** The owner-credential fallback in `voice_realtime_live_test.dart` is removed; live mode fails fast naming NOVA_LIVE_EMAIL/_PASSWORD. The legacy `server/` seed no longer creates `admin123` (env-required, never logged). Was, for the record: `apps/mobile/test/integration/voice_realtime_live_test.dart` (tracked; repo is **PUBLIC**) ships a fallback `admin@nova.leadup.in` + a real-looking password. The account exists in production and holds the **Owner** role binding. Mitigating fact, verified: the published password returns **401 INVALID_CREDENTIALS** (already rotated). | Test convenience default never removed | **P1 (security)** | **FIXED (repo side)** — history scrub still owed | Still owed: rotate that password again, rewrite history (`git filter-repo`) or accept the leak of a stale credential | An Owner e-mail + old password were published world-readable |
| 60 | Production exercised by a real device | At least one handset end-to-end against prod | **VERIFIED ABSENT.** Live DB: 30 users (28 synthetic e-mail domains), **devices = 0**, subscriptions = 0, 4 conversations, 38 messages, 11 usage_records, DB = 21 MB. All handset testing this programme used `:3099` + `adb reverse`. | No prod smoke pass ever ran | **P1** | OPEN | One scripted prod smoke (register → OTP → voice turn → reminder → delete account) on the handset | The deployed stack is proven only by 401s and /healthz |
| 61 | Recovery posture | Backups exist, are off-host, and restores are drilled | **SUBSTANTIALLY FIXED 2026-09-25 — first restore ever proven.** `deploy/backup-db.sh` (now versioned in the repo, deployed to /opt/nova/backup-db.sh, wired into cron) backs up PostgreSQL **plus** the MinIO and Redis volumes (alpine helper over the named volumes, so it needs no registry access), verifies every archive (gzip/tar integrity, non-empty, schema present for the dump, 600 perms), prunes at 14 days, enforces a free-space floor, and exits non-zero on any failure. **Restore drill:** the newest dump was restored into a scratch database — 57/57 schema objects, pgvector present, row counts identical to production across users/sessions/organizations/conversations/tasks/reminders/memories/usage_records/audit_logs/subscriptions, real content present (30 users with e-mail), 1s wall time, zero SQL errors, scratch database dropped afterwards. The drill is now automated weekly (Sundays 04:15 UTC via `RESTORE_DRILL=1`) and available ad hoc. **Off-host:** all three archive types were copied to an off-host disk and verified byte-identical by sha256; the script can push automatically via `OFFHOST_TARGET` (rsync/scp) and prints an explicit WARNING while that is unset — which it still is in cron. **Remaining:** off-host is still manual (row 65), and MinIO/Redis archives are integrity-checked but not restore-drilled (both currently hold no object payloads — see row 64). | One-host deployment | **P2** | **PARTIALLY FIXED** — drill proven + coverage added; off-host automation open (row 65) | Set `OFFHOST_TARGET` in the cron entry; extend the drill to the MinIO/Redis archives once they hold real objects | A disk/volume loss now costs at most a day, but still takes the daily copies with it |
| 62 | Documentation accuracy | A new engineer can trust the docs | **VERIFIED MISLEADING.** `docs/runbook.md` names 13 services that do not exist and Expo/Metro/kubectl that are not used; `README.md` instructs `npm install` + scripts that do not exist; `docs/operations/monitoring.md` documents a Prometheus `/metrics` surface that no code serves (no exporter dep anywhere); `HOW-TO-GET-APK.md` describes the dead EAS pipeline; `CURRENT_ARCHITECTURE.md`/`CLAUDE.md` still cite the dead `api.nova.leadup.tech` host (actual: `nova.leadup.in`, verified live). ~14 of 45 docs are stale or aspirational. | No doc retirement practice | **P2** | OPEN | One triage pass: banner or bin the stale set; make the round-report format the only living register | Anyone — human or agent — acts on false instructions |
| 63 | Dead/unshipped service code | Only deployed services in the tree | **VERIFIED.** `services/` contains `agent-orchestrator`, `voice-api`, `worker`, `workers`, `api_disabled`, `notifications` with no presence in the live compose (compose runs api, auth, realtime-gateway, notification-service, integration-service, workflow-engine, admin, admin-ui + infra). Dist/compiled artefacts committed under several services. | Failed consolidation | **P3** | OPEN | Delete or archive unshipped services; confirm none is referenced by deploy/ scripts | False surface area for audit, install and CI |
| 64 | Audio recording persistence | Upload → object store → retrieval proven with real audio | **FIXED & DEPLOYED 2026-09-25.** The store→row→read-back path was exercised for the first time with real audio bytes against a real MinIO and the canonical schema: a 32,044-byte WAV and a 20,000-byte M4A were uploaded and stored (`recordings/<user>/<id>.{wav,m4a}`, correct ContentType, checksum in the row), and both were read back byte-identical (sha256 `7c1d5314…`, `2347ec15…`) through S3 GetObject, the app's own `getAudio()`, and the HTTP route. **Defect found and fixed (D1):** `GET /recordings/:id/audio` — declared by the mobile client as the read half of the upload route — **was never registered** and is absent from the entire git history, so every download of audio the server held returned 404. Added with ownership scoping (missing/soft-deleted/foreign row → 404, so a foreign recording's existence is never confirmed), missing object → 404, unreachable store → 503. **Defect found and fixed (D2):** `POST /recordings` returned the whole row, publishing `storageKey` and `tenantId` while list/detail/upload used the documented projection; it now uses `toClientRecording` (verified live: neither field in the response; the Dart model parses only the public set). 5 new tests pin the route; suite **70 files / 1130 passed**, `tsc` clean. Deployed to nova.leadup.in (route confirmed present in the running image; live unauthenticated GET now 401 where it previously fell through to 404). | Capability had never been run with real audio | **P1** | **FIXED & DEPLOYED** — see row 66 for two unfixed minors | Re-run once real user recordings exist in production; row 60's device smoke should record and play back audio | "Recordings work" no longer rests on reading the code |
| 65 | Off-host backup automation | Daily archives leave the host without human action | **OPEN.** `deploy/backup-db.sh` supports `OFFHOST_TARGET` (rsync, scp fallback) and warns loudly when it is unset, but the cron entry does not set it, so the nightly copies remain on the same disk as the database. A manual off-host pull to `/Volumes/External/nova-backups` was performed and checksum-verified for all three archive types. | No second host or object store provisioned | **P2** | OPEN | Set `OFFHOST_TARGET` in cron, or add a pull-side job on an always-on machine | Daily backups still die with the host — the manual copy is a human habit, not a control |
|| 66 | Recording subsystem loose ends | No known race or ignored config in the audio path | 
| 67 | Observability | Metrics and error reporting an operator can act on | **METRICS IMPLEMENTED & DEPLOYED 2026-09-25; error reporting still absent.** `docs/operations/monitoring.md` documented a `/metrics` endpoint, a metric catalogue and alerting rules, but no exporter, registry or route existed — a scrape configured from that document could never succeed. Now served (`services/api/src/metrics/registry.ts`): `http_requests_total{method,path,status}`, `http_request_duration_seconds` (labelled by Express route pattern, never raw URL — raw URLs would create a series per recording id), `db_connection_pool_active|idle|waiting` read from the live pool, `nova_build_info{version,commit}`, and prom-client's process/runtime defaults. Access is private-address-only, or a timing-safe `METRICS_TOKEN` bearer check; production publishes the port on loopback and the public URL redirects to the admin login, so nothing is exposed. Two bugs were found building it and are pinned by tests: an unreadable pool counter produced `NaN`, which prom-client rejects — the rejection escaped an async Express 4 handler and every scrape hung until timeout (gauges now emit no sample for an unknown value, and the handler answers 503 rather than hanging); and labels read after Express unwound the router stack collapsed to `/:id`, merging every unmatched route into one series. Live in production: HTTP 200 `text/plain; version=0.0.4`, 37 series families, `nova_build_info` reporting the exact deployed commit `2b4ae31`. **Still missing:** no Sentry or any error reporting (`SENTRY_DSN` unset, zero code references) — exceptions are only visible in container logs; and the doc's Redis, auth, provider and query-timing series remain uninstrumented, now listed explicitly so the alerting rules over them are visibly not coverage. | Docs described an exporter nobody wrote; no error reporting at all | **P2** | **PARTIAL** — metrics done, error reporting open | Wire an error reporter (or accept log-only and delete the claims), then instrument the documented-but-missing series or drop the rules | An operator can now see request rate, latency, pool pressure and build identity, but not a stack trace |
